{
  "rules": {
    "no-unused-vars": "off", // temporary disabled to silence the current JSDoc bug, plus VS Code already highlights unused variables
    "no-unused-expressions": ["warn", { "allowShortCircuit": true }] // for allowIncompleteVariations || console.log()
  }
}
